# goes to url and fetches list of events
def events_list_site(url):
    return

# goes to monitor file and fetches list of events
def events_list_file(monitor_file):
    return

# checks an event is a concert
def is_concert(event):
    return
